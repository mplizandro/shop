spring.application.name=apishop

spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=${DB_PLATFORM}

server.port=${SERVER_PORT:8080}

# Esta configuracion es util cuando se hace pruebas o implementacion para puestos de trabajo
# ================================
# DATASOURCE (H2)
# ================================

# spring.datasource.url=jdbc:h2:mem:shopdb;MODE=MySQL;DB_CLOSE_DELAY=-1
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=samadai
# spring.datasource.password=

# ===============================
# JPA / HIBERNATE
# ===============================
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

# Para que H2 se porte como MySQL (importante para tests)
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# ===============================
# H2 CONSOLE
# ===============================
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console